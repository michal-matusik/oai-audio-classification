# English task summary

Classify short audio clips in the original third-edition first-stage task.
The official notebook supplied a train/validation interface and evaluated accuracy on a hidden set.
The reconstruction uses log-spectrogram features, robust normalization, and augmentation suitable for a compact CNN.
